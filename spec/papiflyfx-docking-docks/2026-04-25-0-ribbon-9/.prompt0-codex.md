# Prompt - Implement Ribbon Side Placement

Use this prompt to start the implementation session for `spec/papiflyfx-docking-docks/2026-04-25-0-ribbon-9`.

```text
As @core-architect, lead this PapiflyFX Docking task.

Read these first:
- AGENTS.md
- spec/agents/README.md
- spec/agents/playbook.md
- spec/agents/core-architect.md
- spec/agents/ui-ux-designer.md
- spec/agents/qa-engineer.md
- spec/agents/spec-steward.md
- spec/papiflyfx-docking-docks/2026-04-25-0-ribbon-9/README.md
- spec/papiflyfx-docking-docks/2026-04-25-0-ribbon-9/design.md
- spec/papiflyfx-docking-docks/2026-04-25-0-ribbon-9/plan.md
- spec/papiflyfx-docking-docks/2026-04-25-0-ribbon-9/progress.md

Task:
Implement Ribbon 9 side placement for `RibbonDockHost` and `Ribbon`.

Priority:
P2 (Normal)

Implementation lead:
@core-architect

Required reviewers:
@ui-ux-designer, @qa-engineer, @spec-steward

Impacted modules:
- papiflyfx-docking-docks
- papiflyfx-docking-samples
- spec/**

Consult if scope expands:
- @feature-dev if provider contracts or feature-module ribbon providers change
- @ops-engineer if Maven/TestFX configuration, archetype templates, or release/build behavior changes

Expected implementation:
1. Add a `RibbonPlacement` enum with `TOP`, `LEFT`, `RIGHT`, and `BOTTOM`.
2. Add `placementProperty()`, `getPlacement()`, and `setPlacement(...)` to `RibbonDockHost` and `Ribbon`.
3. Keep `TOP` as the default and preserve current host behavior when placement is not set.
4. Make `RibbonDockHost` attach the ribbon to the matching `BorderPane` side while keeping dock content in the center.
5. Make `Ribbon` render horizontal layouts for `TOP` and `BOTTOM`, and vertical layouts for `LEFT` and `RIGHT`.
6. Keep ribbon providers placement-agnostic. Do not add side-specific provider SPI.
7. Preserve minimized behavior across all placements, including minimized tab activation that shows the selected tab command panel without changing the persisted minimized flag.
8. Make adaptive group sizing orientation-aware: width for horizontal placements, height for vertical placements.
9. Persist optional `placement` in ribbon session state. Missing, unknown, or malformed placement must fall back to `TOP` without invalidating minimized, selected-tab, or QAT state.
10. Add placement/orientation style classes and tokenized CSS using existing `-pf-ui-*` and `-fx-ribbon-*` variables.
11. Preserve readable non-rotated labels, visible focus indicators, tab toggle-group behavior, QAT behavior, and menu/split-button readability.
12. Add a deterministic `SamplesApp` catalog demo that compares `TOP` and `LEFT` ribbon hosts using the same representative local sample commands.
13. Add focused headless tests for default compatibility, explicit placements, session compatibility, CSS classes, minimized activation, sample catalog registration, and sample build.
14. Update docs and `progress.md` as implementation phases complete.

Key invariants:
- Existing applications that do not set placement render a top ribbon.
- Existing saved sessions without placement restore successfully as `TOP`.
- Feature modules and `RibbonProvider`s remain placement-agnostic.
- QAT persistence remains id-first.
- Selected tab and minimized state semantics remain compatible with Ribbon 8 behavior.
- Vertical placement labels must stay readable and must not be rotated.
- CSS must use existing shared tokens rather than hard-coded one-off colors.
- Samples must be deterministic and local-only; do not require credentials, network access, browser launches, real repository mutation, or external CLIs.

Open questions to resolve before or during Phase 1:
1. Should placement be host-configured only for this slice, or should a built-in ribbon command let users switch placement?
2. Should side ribbons use a fixed rail width, a theme token, or derive width from visible groups?
3. Should bottom placement mirror top header ordering or place QAT nearest the content edge?

Use conservative defaults if no broader product decision exists:
- Keep placement host-configured only.
- Use a tokenized side-rail width with responsive constraints.
- Let bottom placement mirror top header ordering unless implementation exposes a clear usability issue.

Validation expectations:
- `./mvnw -pl papiflyfx-docking-docks -am -Dtest=Ribbon*Test,Ribbon*FxTest -Dsurefire.failIfNoSpecifiedTests=false -Dtestfx.headless=true test`
- `./mvnw -pl papiflyfx-docking-samples -am -Dtest=*Ribbon*FxTest,SamplesSmokeTest -Dsurefire.failIfNoSpecifiedTests=false -Dtestfx.headless=true test`
- `git diff --check`

Acceptance criteria:
- `RibbonDockHost` supports `TOP`, `BOTTOM`, `LEFT`, and `RIGHT` placement.
- `Ribbon` updates header, tab strip, command panel, scroller axis, style classes, and minimized chrome for each placement.
- Default `TOP` behavior and existing sessions remain compatible.
- Ribbon session persistence includes optional placement and tolerates invalid placement values.
- Feature `RibbonProvider`s need no placement-specific changes.
- Vertical ribbons keep labels readable, command groups accessible, and focus indicators visible.
- `SamplesApp` includes a deterministic top/left ribbon comparison demo.
- Focused headless tests pass.

Close with:
1. Files changed
2. Validation commands and results
3. Any remaining risks
4. Handoff block using the repository handoff contract
```
