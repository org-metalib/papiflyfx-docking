# Agent 3 — `@qa-validator`: Verify Settings Demo UI Fix

**Prerequisite:** Agents 1 and 2 must have completed their changes.

**Module scope:** `papiflyfx-docking-settings` + `papiflyfx-docking-samples` (read-only verification, no code changes)

## Task 1 — Compile both modules

```bash
./mvnw -pl papiflyfx-docking-settings,papiflyfx-docking-samples -am clean compile
```

**Pass criteria:** zero compilation errors.

## Task 2 — Run tests headless

```bash
./mvnw -pl papiflyfx-docking-settings,papiflyfx-docking-samples -am -Dtestfx.headless=true test
```

**Pass criteria:** all tests pass. If there are test failures, report the failing test class, method, and
the first 30 lines of the failure output so the responsible agent can fix it.

## Task 3 — Static verification checklist

Verify the following by reading the modified source files:

### SettingsPanel.java
- [ ] `applyThemeTokens()` called at end of constructor with initial theme
- [ ] `themeListener` body calls `applyThemeTokens(newTheme)` (not a no-op comment)
- [ ] `setStyle(...)` includes both `UiCommonThemeSupport.themeVariables(palette)` and `UiStyleSupport.metricVariables()`
- [ ] `paletteFromTheme()` builds `UiCommonPalette` from all required fields (15 constructor args)
- [ ] No hardcoded hex colors remain in `SettingsPanel.java`

### SettingsToolbar.java
- [ ] `UiCommonStyles.ensureLoaded(this)` called in constructor
- [ ] Apply/Reset buttons are `UiPillButton` (not plain `Button`)
- [ ] `scopeSelector` has style class `"pf-ui-compact-field"`
- [ ] `dirtyLabel` and `statusLabel` wrapped in `UiStatusSlot`
- [ ] Dynamic action buttons in `setActions()` use `UiPillButton`

### SettingsSearchBar.java
- [ ] `UiCommonStyles.ensureLoaded(this)` called in constructor
- [ ] `searchField` has style class `"pf-ui-field"`

### SamplesApp.java
- [ ] No hardcoded hex color strings remain (`#3c3c3c`, `#1e1e1e`, `#888888`, `#5b6470`, `#0e639c`, `#bbbbbb`)
- [ ] `NAVIGATION_THEME` static field removed, replaced with `buildNavigationTheme(Theme)` method
- [ ] `themeProperty` listener re-styles top bar, content area, and navigation tree
- [ ] `renderNavigationCell` uses theme-derived colors for category rows
- [ ] `UiCommonThemeSupport` is the only color derivation source (no new hardcoded colors introduced)

## Task 4 — Report

Produce a short pass/fail summary per checklist item. If any item fails, include the file path, line number,
and what needs to change so the owning agent can fix it in a follow-up pass.
